# Copy to .env.local for local development (never commit secrets).

# Public site URL (used later for absolute links / cookies)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Node production port (Hostinger often injects PORT)
# PORT=3000

# Future: operator notify, session secrets (T5+)
# SESSION_SECRET=
# OPERATOR_NOTIFY_EMAIL=
